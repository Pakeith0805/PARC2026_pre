# 提出履歴

提出したzipと、それを作ったコード・結果の対応表。zip本体は`submissions/`に
置くが`.gitignore`済みなので、**どのzipがどのコミットから作られたかはここでしか
辿れない**。提出したら必ず1行追加すること。

zipの再構築手順は[submission_template/README.md](submission_template/README.md)、
判断の経緯は[my_strategy.md](my_strategy.md)、技術的な分析は
[competition_analysis.md](competition_analysis.md)を参照。

| # | 日付 | zip | コミット | 変更点 | 結果 |
|---|---|---|---|---|---|
| 1 | 2026-08-02 | （保存なし） | — | lerobot 0.3系をpipインストール | **失敗**: Python 3.10で入らない（方針5） |
| 2 | 2026-08-04 | （保存なし） | — | lerobot 0.4.4に固定 | **失敗**: `evdev`のビルドエラー、`Python.h`が無い（方針6） |
| 3 | 2026-08-05 | `submission_smolvla_base_2026-08-04.zip` | `f5a1034` | lerobotをvendor同梱、モデル重みをzip同梱 | **失敗**: サーバー起動失敗、`LocalEntryNotFoundError`（方針7） |
| 4 | 2026-08-06 | `submission_smolvla_base_2026-08-06.zip` | （#3 + 方針7の修正、未コミットで作成） | HFキャッシュの環境変数を上書き代入＋スナップショット直参照 | **完走**: 起動36秒。ただし `public_score 0.000`。8エピソード全てが300ステップ消化＝ゴール未到達（方針8） |
| 5 | 2026-08-06 | `submission_smolvla_nstep10_2026-08-06.zip` | `0897024` | `n_action_steps` 50→10 | **初の非ゼロ**: `public_score 0.12839`。ローカルのT1 exampleでは70.0%だった |

### #5 のローカル70% vs 本番12.8% のギャップについて

ローカルで測ったのは`compe/t1`の**exampleタスク4種**で、本番の採点は
**holdoutタスク**（`compe/t1/holdout_test_tasks.csv`、15件）で行われる。別物なので
そのまま比較できない。加えて本番は`3_Omni`＝全摂動の合成という最も厳しい条件で、
成功判定も「ゴール達成 **かつ** 対象外物体の変位1mm以下」と厳しい。

したがって「ローカルの70%が本番で12.8%に落ちた」のではなく、**そもそも別の
指標を見ている**。今後はローカル値を本番の予測値として扱わず、
「変更の効果の向き（上がったか下がったか）」の判断にだけ使うこと。

## 記録するときの注意

- **#5 のzipは `0897024` の内容そのもの**。その後の `a6f9286`
  （`SMOLVLA_FORCE_ONLINE` の追加）は重み再生成の経路だけを変えるもので、
  `SMOLVLA_FORCE_ONLINE` 未設定時＝採点環境での挙動は同一（敵対的条件での
  ロード・推論を実機確認済み）。
- **コミットしてからzipを作る**。#4はコミット前にzipを作ったため、そのzipに
  対応するツリーがgitに残っていない（内容は#5のコミットに含まれる方針7の
  修正と同一だが、厳密な再現はできない）。
- 本番のスコアは`public_score`をそのまま書く。ローカルの測定値と混ぜない
  （T1 exampleタスクと本番のholdoutタスクは別物なので、ローカル70%が本番70%を
  意味しない）。
- 失敗した場合はエラーの一行と、対応する`my_strategy.md`の方針番号を書く。
